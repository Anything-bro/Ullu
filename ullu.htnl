

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ðŸŽ‚ Happy Birthday!</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Quicksand', sans-serif;
            background: linear-gradient(135deg, #ffd1dc 0%, #ffb3d9 50%, #c9a3f7 100%);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
            padding: 10px;
        }

        .float-item {
            position: fixed;
            font-size: 35px;
            animation: floatAround 8s infinite ease-in-out;
            z-index: 1;
            pointer-events: none;
        }

        @keyframes floatAround {
            0%, 100% { 
                transform: translateY(100vh) translateX(0) rotate(0deg); 
                opacity: 0; 
            }
            10% { opacity: 1; }
            50% { 
                transform: translateY(50vh) translateX(30px) rotate(180deg); 
                opacity: 1; 
            }
            90% { opacity: 1; }
            100% { 
                transform: translateY(-50px) translateX(-30px) rotate(360deg); 
                opacity: 0; 
            }
        }

        .container {
            max-width: 500px;
            margin: 20px auto;
            position: relative;
            z-index: 10;
        }

        .card {
            background: rgba(255, 255, 255, 0.98);
            border-radius: 25px;
            padding: 25px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
            animation: cardSlide 0.8s ease-out;
            text-align: center;
        }

        @keyframes cardSlide {
            from { 
                transform: translateY(50px) scale(0.9); 
                opacity: 0; 
            }
            to { 
                transform: translateY(0) scale(1); 
                opacity: 1; 
            }
        }

        .photo-container {
            width: 200px;
            height: 200px;
            margin: 0 auto 20px;
            border-radius: 50%;
            overflow: hidden;
            border: 6px solid #ff69b4;
            box-shadow: 0 10px 30px rgba(255, 105, 180, 0.4);
            animation: photoZoom 1s ease-out;
            position: relative;
        }

        @keyframes photoZoom {
            from { transform: scale(0) rotate(180deg); }
            to { transform: scale(1) rotate(0deg); }
        }

        .photo-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .sparkle-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            animation: sparkleMove 3s infinite;
            pointer-events: none;
        }

        @keyframes sparkleMove {
            0%, 100% { 
                background: radial-gradient(circle at 20% 20%, rgba(255,255,255,0.8) 0%, transparent 50%);
            }
            50% { 
                background: radial-gradient(circle at 80% 80%, rgba(255,255,255,0.8) 0%, transparent 50%);
            }
        }

        h1 {
            font-size: 2.2em;
            color: #ff1493;
            margin: 15px 0;
            font-weight: 700;
            animation: colorPulse 3s infinite;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }

        @keyframes colorPulse {
            0%, 100% { color: #ff1493; }
            33% { color: #ff69b4; }
            66% { color: #ff6347; }
        }

        .name {
            font-size: 1.8em;
            color: #c71585;
            font-weight: 600;
            margin: 10px 0;
        }

        .message {
            font-size: 1.1em;
            color: #8b008b;
            margin: 15px 0;
            line-height: 1.6;
        }

        .emoji-line {
            font-size: 2em;
            margin: 20px 0;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0) scale(1); }
            50% { transform: translateY(-15px) scale(1.1); }
        }

        .animals {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin: 20px 0;
            flex-wrap: wrap;
        }

        .animal {
            font-size: 3em;
            animation: animalDance 1.5s infinite;
            cursor: pointer;
            transition: transform 0.3s;
        }

        .animal:nth-child(1) { animation-delay: 0s; }
        .animal:nth-child(2) { animation-delay: 0.2s; }
        .animal:nth-child(3) { animation-delay: 0.4s; }
        .animal:nth-child(4) { animation-delay: 0.6s; }

        @keyframes animalDance {
            0%, 100% { transform: rotate(0deg) scale(1); }
            25% { transform: rotate(-10deg) scale(1.1); }
            75% { transform: rotate(10deg) scale(1.1); }
        }

        .animal:active {
            transform: scale(1.3) rotate(20deg);
        }

        .btn {
            background: linear-gradient(135deg, #ff6b9d, #ffa6c9);
            color: white;
            border: none;
            padding: 15px 35px;
            font-size: 1.2em;
            border-radius: 30px;
            cursor: pointer;
            margin: 15px 0;
            font-family: 'Quicksand', sans-serif;
            font-weight: 600;
            box-shadow: 0 8px 20px rgba(255, 107, 157, 0.4);
            animation: btnPulse 2s infinite;
            transition: all 0.3s;
        }

        @keyframes btnPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .btn:active {
            transform: scale(0.95);
        }

        .hidden {
            display: none;
            background: linear-gradient(135deg, #fff0f5, #ffe4e9);
            padding: 20px;
            border-radius: 20px;
            margin: 15px 0;
            border: 3px dashed #ff69b4;
            font-size: 1.1em;
            color: #c71585;
            animation: popIn 0.5s;
        }

        @keyframes popIn {
            from { transform: scale(0); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }

        @media (max-width: 480px) {
            h1 { font-size: 1.8em; }
            .name { font-size: 1.5em; }
            .message { font-size: 1em; }
            .photo-container { 
                width: 160px; 
                height: 160px; 
            }
            .animal { font-size: 2.5em; }
            .btn { 
                padding: 12px 25px; 
                font-size: 1.1em; 
            }
        }

        @media (max-width: 360px) {
            h1 { font-size: 1.5em; }
            .photo-container { 
                width: 140px; 
                height: 140px; 
            }
        }
    </style>
</head>
<body>
    <div id="floaters"></div>

    <div class="container">
        <div class="card">
            <!-- EDIT: Replace with her photo URL -->
            <div class="photo-container">
                <img src="https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=400" alt="Birthday Girl">
                <div class="sparkle-overlay"></div>
            </div>

            <h1>ðŸŽ‚ Happy Birthday! ðŸŽ‰</h1>

            <!-- EDIT: Change name here -->
            <div class="name">
                Your Name Here
            </div>

            <div class="emoji-line">âœ¨ ðŸŽˆ ðŸ’– ðŸŽ€ âœ¨</div>

            <!-- EDIT: Change message here -->
            <div class="message">
                You make every day special!<br>
                Hope your birthday is as amazing as you are! ðŸ’•
            </div>

            <div class="animals">
                <div class="animal" onclick="makeSound('ðŸ°')">ðŸ°</div>
                <div class="animal" onclick="makeSound('ðŸ»')">ðŸ»</div>
                <div class="animal" onclick="makeSound('ðŸ¼')">ðŸ¼</div>
                <div class="animal" onclick="makeSound('ðŸ¦Š')">ðŸ¦Š</div>
            </div>

            <button class="btn" onclick="showMessage()">
                ðŸ’ Special Message
            </button>

            <!-- EDIT: Change secret message here -->
            <div class="hidden" id="secret">
                <h3 style="color: #ff1493; margin-bottom: 10px;">ðŸ’•</h3>
                Thanks for being you!<br>
                Have the best day ever! ðŸŒŸ
            </div>
        </div>
    </div>

    <script>
        function createFloaters() {
            const container = document.getElementById('floaters');
            const items = ['ðŸŽˆ', 'ðŸŽ€', 'ðŸ’–', 'âœ¨', 'ðŸŒ¸', 'ðŸ¦‹', 'â­', 'ðŸ’', 'ðŸŽ'];

            setInterval(() => {
                const item = document.createElement('div');
                item.className = 'float-item';
                item.innerText = items[Math.floor(Math.random() * items.length)];
                item.style.left = Math.random() * 100 + '%';
                item.style.animationDuration = (Math.random() * 4 + 6) + 's';
                item.style.animationDelay = Math.random() * 2 + 's';
                container.appendChild(item);

                setTimeout(() => item.remove(), 10000);
            }, 1200);
        }

        function showMessage() {
            const msg = document.getElementById('secret');
            msg.style.display = 'block';

            const container = document.getElementById('floaters');
            for (let i = 0; i < 30; i++) {
                setTimeout(() => {
                    const confetti = document.createElement('div');
                    confetti.className = 'float-item';
                    confetti.innerText = ['ðŸŽ‰', 'âœ¨', 'ðŸ’–'][Math.floor(Math.random() * 3)];
                    confetti.style.left = Math.random() * 100 + '%';
                    confetti.style.animationDuration = '3s';
                    container.appendChild(confetti);
                    setTimeout(() => confetti.remove(), 3000);
                }, i * 50);
            }
        }

        function makeSound(animal) {
            const sounds = {
                'ðŸ°': 'Hop hop! ðŸ°',
                'ðŸ»': 'Rawr! ðŸ»',
                'ðŸ¼': 'Munch munch! ðŸ¼',
                'ðŸ¦Š': 'What does the fox say? ðŸ¦Š'
            };

            const msg = document.createElement('div');
            msg.style.cssText = 'position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);background:white;padding:20px 40px;border-radius:20px;font-size:1.5em;box-shadow:0 10px 30px rgba(0,0,0,0.3);z-index:1000;animation:popIn 0.3s;';
            msg.innerText = sounds[animal];
            document.body.appendChild(msg);

            setTimeout(() => msg.remove(), 1000);
        }

        createFloaters();
    </script>
</body>
</html>
